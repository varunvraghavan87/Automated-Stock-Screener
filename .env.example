# Supabase Auth (required for user authentication)
# Get these from https://supabase.com/dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Encryption key for Kite API secrets stored in database (required)
# Each user provides their own Kite API Key + Secret via the Settings UI.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
KITE_CREDENTIALS_ENCRYPTION_KEY=your_64_char_hex_key_here
